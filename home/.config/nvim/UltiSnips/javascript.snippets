snippet foril "for (let i...) {...}"
for (let ${1:i} = 0, ${2:len} = ${3:Things.length}; $1 < $2; $1++) {
	${0:${VISUAL}}
}
endsnippet

snippet rffc "React Functional Component ()=>" b
import React from 'react';
import PropTypes from 'prop-types';
import styles from './${2:$1}.css';

const ${1:`!v expand('%:t:r')`} = (${3:{...props}}) => {
	return (
			<div className={styles.base}>
			$4
			</div>
    );
}

$1.defaultProps = {};

$1.propTypes = {};

export default $1;
endsnippet
